<UserControl x:Class="SightWordCards.Card"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SightWordCards"
             mc:Ignorable="d" 
             Height="450" Width="800">
    <UserControl.Effect>
        <DropShadowEffect Direction="270" BlurRadius="40" Opacity="0.4" ShadowDepth="10"/>
    </UserControl.Effect>
    <Grid>
        <Border x:Name="BgBorder" CornerRadius="30" BorderBrush="#44000000" BorderThickness="2" Background="White"/>
        <Border CornerRadius="30">
            <Border.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#00000000" Offset="0"/>
                    <GradientStop Color="#19000000" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <Viewbox>
            <Label x:Name="WordBox" x:FieldModifier="private"/>
        </Viewbox>

    </Grid>
</UserControl>
